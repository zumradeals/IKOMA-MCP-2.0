# IKOMA MCP — Package (conceptuel)

Ce package expose des contrats, types et frontières alignés sur les Actes I–IV.

## Contenu

- `src/ikoma_mcp/` : code conceptuel (interfaces, types, états, évidences).
- `tests/` : réservé pour plus tard (peut rester vide en BUILD-2).

Aucune exécution n’est incluse.
