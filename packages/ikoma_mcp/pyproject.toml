[project]
name = "ikoma_mcp"
version = "0.1.0"
description = "IKOMA MCP 2.0 — package conceptuel (BUILD-2)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Proprietary" }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
